# Copy this file to .env and change the values if the default port is already in use.
# SCANNERHUB_PORT wins over data/settings.json. --port on the command line wins over both.

SCANNERHUB_HOST=127.0.0.1
SCANNERHUB_PORT=8767
